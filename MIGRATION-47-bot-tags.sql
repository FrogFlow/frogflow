-- Теги на клиента — структурированные метки поверх свободного текста notes
-- («горячий лид», «риск оттока», «VIP» и т.п.), чтобы фильтровать список в
-- панели оператора, а не искать по подстроке в заметках.

ALTER TABLE public.bots ADD COLUMN IF NOT EXISTS tags TEXT[] NOT NULL DEFAULT '{}'::text[];

COMMENT ON COLUMN public.bots.tags IS
  'Теги оператора для фильтрации в панели — произвольные короткие метки, задаются вручную.';
