-- ═══════════════════════════════════════════════════════════════════════════
-- Удалить мёртвую схему Instagram-автоматизации прошлого поколения.
--
-- Семь таблиц не упоминаются в коде НИ ОДНОЙ строкой — только в
-- сгенерированном src/integrations-supabase/types.ts (сверено ANALYSIS.md и
-- отдельно перепроверено этой правкой: grep по всему src/ ни разу не находит
-- ig_poll_runs, ig_comment_actions, ig_post_leads, ig_watched_posts,
-- ig_keywords, ig_exclusions, zernio_automations нигде, кроме types.ts).
--
-- Это остатки подхода до перехода на вебхуки Zernio (см. ANALYSIS.md,
-- «Второе обследование»): опрос по расписанию, ретраи с next_retry_at,
-- построенный на другом провайдере (unipile_comment_id в ig_post_leads —
-- слова «unipile» в репозитории нет вовсе). Опрос работал 27.07–01.08,
-- 7452 прогона, дальше код опроса удалили, а таблицы и данные остались.
--
-- Не ошибка, но мусор, который выглядит как рабочая часть системы: следующий,
-- кто откроет схему, потратит время на выяснение, почему ig_poll_runs не
-- пишется.
--
-- ⚠️ ДЕСТРУКТИВНО И НЕОБРАТИМО. Перед применением:
--   1. Снять бэкап/снапшот базы (или хотя бы pg_dump этих семи таблиц) —
--      данные старые и, по всей видимости, никому не нужны, но это не
--      проверялось этой правкой эмпирически, только по коду.
--   2. Убедиться, что grep по актуальному src/ на момент применения
--      действительно не находит обращений (миграции применяются вручную и
--      могут отстать от кода, на котором писалось обоснование выше).
--   3. Применять в Supabase SQL Editor, как и предыдущие миграции — DDL через
--      SUPABASE_SERVICE_ROLE_KEY (ключ Data API) не выполнить.
-- ═══════════════════════════════════════════════════════════════════════════

BEGIN;

DROP TABLE IF EXISTS public.ig_poll_runs;
DROP TABLE IF EXISTS public.ig_comment_actions;
DROP TABLE IF EXISTS public.ig_post_leads;
DROP TABLE IF EXISTS public.ig_watched_posts;
DROP TABLE IF EXISTS public.ig_keywords;
DROP TABLE IF EXISTS public.ig_exclusions;
DROP TABLE IF EXISTS public.zernio_automations;

COMMIT;

-- ─── Проверка после применения ────────────────────────────────────────────
-- 1. `npm run build` (или `scripts/sync-db-types.mjs`, если он в проекте
--    есть под этим именем) — сгенерированный types.ts должен перестать
--    содержать эти семь таблиц, и сборка должна пройти как раньше: код их не
--    использует, значит и не должен был сломаться.
-- 2. В Table Editor / information_schema.tables убедиться, что таблиц
--    больше нет в public.
